<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand text-danger script" href="{{ path('index') }}">Trinity</a>
  <div class="ml-auto">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#Dropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="Dropdown">
    <ul class="navbar-nav">
        <li class="nav-item py-2 pr-3">
            <p class="navbar-text text-white m-0" id="username">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
              Logged as   
              {% if app.user.prenom is defined %} 
                      {{ app.user.prenom }} 
              {% endif %}
              {{ app.user.nom }}
            {% endif %}

            </p>

        </li>
      <li class="nav-item py-2 pr-3">
        <a class="btn btn-info text-white" href="{{ path('gestion') }}">Accueil Gestion</a>
      </li>
      <li class="nav-item py-2 pr-3">
        <a class="btn btn-danger text-white" href="{{ path('app_logout') }}">Déconnexion</a>
      </li>
      </li>
    </ul>
  </div>
</div>
</div>
</nav>