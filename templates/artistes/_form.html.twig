{# 
{{ form_start(form) }}
    {{ form_widget(form) }}
    <!-- <button class="btn btn-info">{{ button_label|default('Enregistrer') }}</button> -->
{{ form_end(form) }}
#}

<div class="container pt-4 mt-4 mb-4">
        <h2>Formulaire d'ajout d'artiste</h2>

            {{ form_start(form) }}
            <div class="form-group">
                <div class="col-md">
                    {{ form_row(form.nom) }}
                </div>
                <small id="name" class="form-text text-muted">
                        {{ form_errors(form) }}
                </small>
            </div>
            <div class="form-group">
                <div class="col-md">
                    {{ form_row(form.prenom) }}
                </div>
                <small id="name" class="form-text text-muted">
                        {{ form_errors(form) }}
                </small>
            </div>
        
            <div class="form-group">
                <div class="col-md">
                    {{ form_row(form.biographie_fr) }}
                </div>
                <small id="name" class="form-text text-muted">
                        {{ form_errors(form) }}
                </small>
            </div>
            <div class="form-group">
                <div class="col-md">
                    {{ form_row(form.biographie_en) }}
                </div>
                <small id="name" class="form-text text-muted">
                        {{ form_errors(form) }}
                </small>
            </div>
         
            {{ form_end(form) }}
            <a href="{{ path('artistes_index') }}">Retour à la liste</a>

</div>
