{% extends 'base.html.twig' %}

{% block title %}Artistes{% endblock %}

{% block body %}
    <h1>Artistes</h1>
        <div class="text-center">
            <a class="btn btn-dark text-center mb-3" href="{{ path('artistes_index') }}">Retour</a>
        </div>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artiste.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ artiste.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ artiste.prenom }}</td>
            </tr>
            <tr>
                <th>Biographie_fr</th>
                <td>{{ artiste.biographieFr }}</td>
            </tr>
            <tr>
                <th>Biographie_en</th>
                <td>{{ artiste.biographieEn }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('artistes_index') }}">back to list</a>

    <a href="{{ path('artistes_edit', {'id': artiste.id}) }}">edit</a>

    {{ include('artistes/_delete_form.html.twig') }}
{% endblock %}
