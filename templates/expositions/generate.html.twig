{% extends 'base.html.twig' %}

{% block title %}Expositions qr codes{% endblock %}

{% block body %}

    <h2> {{exposition.nom}} </h2>
 
   {% for oeuvre in exposition.oeuvres %}
        <div>
            <img src="{{ asset('qr_codes/' ~ oeuvre.titre ~ oeuvre.id ~ '.png') }}"/>
            
            <p>{{ oeuvre.titre}}  
                {% if oeuvre.artiste.nom is defined and oeuvre.artiste.nom is not empty %}
                -  {{ oeuvre.artiste.nom }} 
                {% endif %}
            <p>

        </div>
    {% endfor %}
{% endblock %}